[build]
  ignore = "cd ../../ && npx nx-ignore company-webapp && cd apps/company-webapp"

[build.environment]
  KRATOS_PUBLIC_URL = "not so secret :^)"
  NETLIFY_USE_YARN = "true" 

[context.production.environment]
  KRATOS_PUBLIC_URL = "not so secret"
